
function serialize(value: any, keyOrdering=false) {
  const serialization = Object.create(null);

  function serializeRecursive(value: any) {

  }
}

export function deepEqual<T>(a: T, b: T, keyOrdering=false): boolean {
    const seen = new Set()

    function deepEqualRecursive(a: T, b: T): boolean {

    }

    return deepEqualRecursive(a, b)
}
